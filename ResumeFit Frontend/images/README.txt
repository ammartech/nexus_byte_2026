This folder is reserved for image assets (e.g., optimized icons or screenshots
referenced by the site). The current build uses inline SVG/emoji icons and CSS
to keep the page weight minimal per the sustainability requirements, so no
binary images are required for the site to function.
